const arrays = [
  ['1', '2', '3'],
  [true],
  [4, 5, 6],
];

// 1 - Dada uma matriz, transforme em um array.

function flatten() {
  console.log(arrays);
  return arrays.reduce((arrayAtual, array) => {
    arrayAtual
  })
}

console.log(flatten());